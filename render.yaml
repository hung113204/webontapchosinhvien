services:
  - type: web
    name: student-course-review-web
    runtime: docker
    plan: free
    buildCommand: composer install --no-dev --optimize-autoloader
    healthCheckPath: /
    envVars:
      - key: APP_ENV
        value: production
      - key: APP_DEBUG
        value: "false"
